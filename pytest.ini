[pytest]
markers =
    network: reaches the internet. Deselected by default so the suite runs
        offline and in CI without flaking on somebody else's outage; run the
        marked tests deliberately with `-m network`.
addopts = -m "not network"
